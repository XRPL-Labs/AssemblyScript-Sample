{
  "name": "wasm-node-sample",
  "version": "0.0.1",
  "description": "",
  "main": "run-wasm/as-loader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "asbuild:untouched": "asc assembly/index.ts --debug -b build/untouched.wasm -t build/untouched.wat -d build/wasm.d.ts",
    "asbuild:optimized": "asc assembly/index.ts -O --noAssert -b build/optimized.wasm -t build/optimized.wat -d build/wasm.d.ts",
    "asbuild:custom": "asc assembly/index.ts -O3 --runtime stub --noAssert -b build/optimized.wasm -t build/optimized.wat -d build/optimized.d.ts",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "full": "./build-and-run.sh"
  },
  "author": "Wietse Wind <hi@wietse.com>",
  "license": "MIT",
  "dependencies": {
    "@assemblyscript/loader": "^0.14.12"
  },
  "devDependencies": {
    "assemblyscript": "^0.14.12"
  }
}
